{
  "name": "stack54-monorepo",
  "version": "1.0.0",
  "description": "",
  "author": "Joshua Amaju",
  "license": "ISC",
  "private": true,
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/joshamaju/stack54.git"
  },
  "scripts": {
    "test": "pnpm --filter '!./templates/**' -r test",
    "changeset": "changeset",
    "release": "changeset publish",
    "build": "tsc -b tsconfig.build.json",
    "version": "changeset version && pnpm -r generate-version && git add --all"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "playwright": "^1.41.1",
    "typescript": "^5.3.3"
  }
}
